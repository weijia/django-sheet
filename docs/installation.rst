============
Installation
============

At the command line::

    $ easy_install django_sheet

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_sheet
    $ pip install django_sheet